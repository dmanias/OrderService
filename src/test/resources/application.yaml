spring:
  config:
    import: optional:configserver:http://localhost:9296
  datasource:
    url: jdbc:h2:mem:order
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://trial-5066932.okta.com/oauth2/default
      client:
        registration:
          internal-client:
            provider: okta
            authorization-grant-type: client_credentials
            scope: internal
            client-id: 0oa6elwt2xzvOnA1X697
            client-secret: omnPi1XKFu4_vpn5d7zGlUgN2sxBb-Yf_aRiNWtW
        provider:
          okta:
            issuer-uri: https://trial-5066932.okta.com/oauth2/default


eureka:
  client:
    enabled: false